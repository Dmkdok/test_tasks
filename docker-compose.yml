version: "3.9"

services:
  web:
    build:
      context: .
    image: task-manager-fastapi:latest
    container_name: task-manager
    ports:
      - "8000:8000"
    # Persist SQLite database to host file
    volumes:
      - ./tasks.db:/app/tasks.db
    restart: unless-stopped


